<!doctype html>
<html lang="bn">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Attendance — Mobile Friendly Table</title>
<link rel="stylesheet" href="/DhanKata/dhankata.css" />
 
    <!-- Dev-specific scripts -->
    <script src="/js/eruda.js"></script>
    <script>
      eruda.init(); // Console চালু হবে
    </script>
    <script src="/DhanKata/dhan.js" defer></script>
</head>
<body>
  <div class="wrap">
    <header>
      <div class="logo">AD</div>
      <div>
        <h1>Fri 28 Nov 2025</h1>
        <p class="lead">৬ জন কর্মীর মোট কাজ, উপস্থিতি, টাকা ও পেমেন্ট লেজার দেখুন।</p>
      </div>
    </header>

    <div class="controls">
      <input id="q" class="search" placeholder="কর্মী খুঁজুন (নাম বা আইডি)" />
      <select id="month" aria-label="month select">
        <option value="all">All dates</option>
      </select>
      <button class="btn primary" id="export">Export CSV</button>
    </div>

    <!-- Desktop Table -->
    <div class="table-wrap">
      <table id="mainTable">
        <thead>
          <tr>
            <th>Worker</th>
            <th>Worked Days</th>
            <th>Absent</th>
            <th>Dates Worked</th>
            <th>Total Wage</th>
            <th>Paid</th>
            <th>Due</th>
            <th>Payments</th>
          </tr>
        </thead>
        <tbody id="tableBody">
          <!-- rows injected by JS -->
        </tbody>
      </table>
    </div>

    <!-- Mobile Cards -->
    <div class="cards" id="cards"></div>

    <div class="empty" id="emptyMsg" style="display:none">কোনো ডাটা মেলেনি</div>

    <footer>Designed for mobile first — Simple, clean and responsive.</footer>
  </div>

  <!-- Modal for payment history -->
  <div class="modal-bg" id="modalBg">
    <div class="modal" role="dialog" aria-modal="true">
      <button class="close" id="closeModal">✕</button>
      <h3 id="modalTitle">Payment History</h3>
      <div id="modalContent"></div>
    </div>
  </div>

  <script>
  
  </script>
</body>
</html>
