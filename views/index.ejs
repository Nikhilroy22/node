

<%- include('header', {showSplash}) %>

<style>
  /* tabs index*/
  .tabsindex{display:flex;gap:.5rem;padding:12px;align-items:center;}
  .tabindex{
    background:linear-gradient(180deg, #2a8f43,#1f7a34);
    padding:8px 12px;border-radius:8px;font-weight:700;cursor:pointer;
  }
  .tabindex.inactiveindex{
    opacity:.65;
  color: #fff;
  background:linear-gradient(180deg,#155226,#0f3d18)}
</style>
<div class="tabsindex">
    <div class="tabindex">Live</div>
    <a class="tabindex inactiveindex" href="/bet">Sports</a>
    <div class="tabindex inactiveindex">Upcoming</div>
  </div>

<!--
<div class="home-container">
  <% if (session.user) { %>
  <h1>  <%= data.username %></h1>
   <% } %>

<p>আপনি সফলভাবে লগইন করেছেন।</p>
<p><%= nodeVersion %></p>


  <% if (session.user) { %>
<a href="/logout">Logout</a>
   <% }else{ %>
  <h1><a href="/login">Login</a></h1>
   
   
   <% } %>

  <% if (session.user) { %>
  
<%- include("partials/bottom-nav", { path: "/" }) %> 
  
   <% } %>


<%- include('footer') %>